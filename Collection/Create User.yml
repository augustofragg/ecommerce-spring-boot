info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/users
  body:
    type: json
    data: |-
      {
        "fullName":"Carlos Augusto",
        "address":"Avenida Alberto Algusto",
        "number":"371",
        "complement":"Figueira"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
