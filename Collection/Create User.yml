info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/users
  body:
    type: json
    data: |-
      {
        "fullName":"Joao",
        "address":"Rua A",
        "number":"6",
        "complement":"Copacabana"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
